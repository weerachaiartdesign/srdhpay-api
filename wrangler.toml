name = "srdhpay-api"
main = "/index.js"
compatibility_date = "2026-07-09"

# ตั้งค่า D1 Binding — ต้องระบุชื่อ binding เป็น "DB" ให้ตรงกับโค้ดทุกไฟล์ (env.DB)
# database_id ด้านล่างต้องเป็น UUID ของฐานข้อมูลจริงที่ใช้งานอยู่
[[d1_databases]]
binding = "DB"
database_name = "srdhpay_db"
database_id = "26443676-5af5-4dfe-ad5a-f97ce7862fa4"
